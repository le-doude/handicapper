require "spec_helper"

describe Handicapper do
  it "has a version number" do
    expect(Handicapper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
